=============README==============
Installation:

    git clone git://github.com/kusoayan/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Open vim, and install bundles:
    
    vim
    :BundleInstall
    :q

===============README EOF==================
